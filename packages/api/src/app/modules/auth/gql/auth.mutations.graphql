type Mutation {
  login(input: LoginInput!): LoginPayload
}

type LoginPayload {
  clientMutationId: String
  error: [String!]
  token: String
}

input LoginInput {
  clientMutationId: String
  email: EmailAddress!
  password: String!
}
